name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            apps/frontend/package-lock.json
            apps/backend/package-lock.json

      - name: Generate sitemap
        working-directory: apps/frontend
        run: node scripts/generate-sitemap.mjs

      - name: Build frontend
        working-directory: apps/frontend
        run: |
          npm ci
          npm run build

      - name: Build backend
        working-directory: apps/backend
        run: |
          npm ci
          npm run build

      - name: Prepare artifact
        run: |
          mkdir -p artifact/frontend
          mkdir -p artifact/server
          cp -r apps/frontend/dist artifact/frontend/
          cp -r apps/backend/dist artifact/server/
          cp apps/backend/package.json apps/backend/package-lock.json artifact/server/

      - uses: actions/upload-artifact@v4
        with:
          name: deploy
          path: artifact/

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: deploy
          path: artifact/

      - name: Deploy
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
        run: |
          set -e
          mkdir -p ~/.ssh

          echo "$SSH_KEY" | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          SSH_OPTS="-o StrictHostKeyChecking=no -o IdentitiesOnly=yes -i ~/.ssh/deploy_key -p $SSH_PORT"

          echo "--- backup db ---"
          ssh $SSH_OPTS "$SSH_USER@$SSH_HOST" 'cp /root/qik-anime/server/data/qik-anime.db /root/qik-anime/server/data/qik-anime.db.bak 2>/dev/null; echo ok'

          echo "--- rsync ---"
          rsync -rlgoDzvc -e "ssh $SSH_OPTS" --exclude='server/data/' --exclude='server/uploads/' artifact/ "$SSH_USER@$SSH_HOST:/root/qik-anime/"

          echo "--- reload ---"
          ssh $SSH_OPTS "$SSH_USER@$SSH_HOST" 'cp -r /root/qik-anime/frontend/dist/* /var/www/quickik.ru/ && cd /root/qik-anime/server && npm ci --omit=dev && pm2 reload anime-api && echo ok'

      - name: Ping search engines
        run: |
          curl -s -o /dev/null -w "Google: %{http_code}\n" "https://www.google.com/ping?sitemap=https%3A%2F%2Fquickik.ru%2Fsitemap.xml"
          curl -s -o /dev/null -w "Yandex: %{http_code}\n" "https://webmaster.yandex.ru/ping?sitemap=https%3A%2F%2Fquickik.ru%2Fsitemap.xml"
